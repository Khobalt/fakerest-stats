{
  "name": "fakerest-stats",
  "version": "1.0.0",
  "description": "CLI REST client that queries a user-data endpoint and reports per-city stats",
  "type": "module",
  "bin": {
    "fakerest-stats": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "test": "tsx --test src/*.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "tsx": "^4.19.0",
    "@types/node": "^22.0.0"
  }
}
